---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: podinfo-dev
  namespace: dev
  annotations:
    fluxcd.io/automated: "true"
    filter.fluxcd.io/chart-image: glob:dev-*
spec:
  releaseName: terraria-world-test
  chart:
    git: git@github.com:ammilam/terraria-world-1
    path: terraria/
    ref: master
  values:
    image:
      repository: ryshe/terraria
      tag: latest
    replicaCount: 1
    hpa:
      enabled: false
